#!/usr/bin/env php
<?php

declare(strict_types=1);

use Bus\Framework\ContainerFactory;

require dirname(__DIR__) . '/vendor/autoload.php';

$application = ContainerFactory::application(dirname(__DIR__));

exit($application->run());
