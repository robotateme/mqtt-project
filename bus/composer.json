{
    "name": "mqtt-project/bus",
    "description": "Integration bus: MQTT ingress to Kafka.",
    "type": "project",
    "require": {
        "php": "^8.4 || ^8.5",
        "ext-rdkafka": "*",
        "php-mqtt/client": "^2.3"
    },
    "autoload": {
        "psr-4": {
            "Bus\\": "app/"
        }
    },
    "scripts": {
        "mqtt:consume": "php bin/mqtt-consume.php"
    }
}
